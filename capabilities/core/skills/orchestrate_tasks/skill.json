{
  "schemaVersion": "1",
  "kind": "agentic",
  "name": "orchestrate_tasks",
  "description": "Execute a DAG of tasks in parallel using isolated sub-agent workers. Tasks without dependencies run concurrently; tasks with depends_on wait for their upstream tasks to complete. Use this instead of sequential delegate_sub_agent calls when you have multiple independent or semi-independent tasks.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "objective": {
        "type": "string",
        "description": "High-level objective that this task graph achieves."
      },
      "tasks": {
        "type": "array",
        "description": "Ordered list of task nodes forming a DAG.",
        "items": {
          "type": "object",
          "properties": {
            "id": { "type": "string", "description": "Stable machine-generated UUID for this task." },
            "title": { "type": "string", "description": "Human-readable title." },
            "depends_on": {
              "type": "array",
              "items": { "type": "string" },
              "description": "IDs of tasks that must complete before this one starts."
            },
            "instruction": { "type": "string", "description": "Clear task instructions for the sub-agent worker." },
            "tool_names": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Tool names the worker may use."
            },
            "skill_names": {
              "type": "array",
              "items": { "type": "string" },
              "description": "Skill names the worker may use (optional)."
            },
            "artifactPath": {
              "type": "string",
              "description": "Path under workspace/ where the worker should write its full output."
            }
          },
          "required": ["id", "title", "instruction"]
        }
      },
      "failFast": {
        "type": "boolean",
        "description": "When true, cancel all remaining tasks on first failure. Default: false."
      },
      "maxConcurrency": {
        "type": "number",
        "description": "Maximum parallel workers. Default: 3."
      }
    },
    "required": ["objective", "tasks"]
  }
}
