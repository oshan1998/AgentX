{
  "schemaVersion": "1",
  "kind": "agentic",
  "model": "gemini-2.5-pro",
  "name": "plan_steps",
  "description": "Break an objective into an ordered task plan (DAG) with dependencies, statuses, and tool assignments. Independent tasks can later be run in parallel via orchestrate_task_graph.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "objective": {
        "type": "string",
        "description": "Goal to break into ordered steps with stable task ids."
      }
    },
    "required": ["objective"]
  },
  "toolNames": [
    "read_task_plan",
    "write_task_plan",
    "patch_task_plan_task"
  ],
  "maxIterations": 12,
  "deadlineMs": 120000
}
