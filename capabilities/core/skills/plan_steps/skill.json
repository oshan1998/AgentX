{
  "schemaVersion": "1",
  "kind": "agentic",
  "name": "plan_steps",
  "description": "Break an objective into ordered steps with dependency relationships (DAG) and persist them to the session task plan (ids, statuses, depends_on, tool_names, optional notes and artifact_path pointers). Independent tasks can be executed in parallel via orchestrate_task_graph.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "objective": {
        "type": "string",
        "description": "Goal to break into ordered steps with stable task ids."
      }
    },
    "required": ["objective"]
  },
  "toolNames": [
    "read_task_plan",
    "write_task_plan",
    "patch_task_plan_task"
  ],
  "maxIterations": 12,
  "deadlineMs": 120000
}
