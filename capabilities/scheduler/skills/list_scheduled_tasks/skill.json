{
  "schemaVersion": "1",
  "name": "list_scheduled_tasks",
  "description": "List all saved scheduled cron tasks.",
  "steps": [
    {
      "type": "tool_call",
      "tool": "list_cron_jobs",
      "saveAs": "jobs"
    },
    {
      "type": "respond",
      "messageTemplate": "{{jobs}}"
    }
  ]
}
